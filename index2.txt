lets see how to push the code from the vs code into git
        if follow this procedure to push the code from the vs code into the github

                        Assume it as a GitFolder=>
                                git copy folder=>working folder=>text1.txt(Untracked)=>not yet moved to the git copy folder
        =>on that time we will get the symbol 'U Untracked'

        =>Use this command they are{ git add index2.txt(file name) }

        =>checkonce using the git status(there they will show the green text)
                                        now its moved to the working folder into the git copy folder
        =>Use this command{ git commit -m "index2.txt"}

        =>now its setuped but in git its not showing because of the we didn't push the git copy folder into the git folder
        =>for that we have to use commands they are 

        =>Use this command they are  {git push origin main}
        this is the process for if we write the code or changed the text in the git means hoe to retriew same command this also

        =>use this command they are (git pull)


                         =>This is the same procedure for the push the code and then modify the code they are
                                                        =>git add index2.txt(file name)
                                                        =>git commit -m "index2.txt file added"
                                                        =>git push origin main

                                                        =>git add index2.ts(file name)
                                                        =>git commit -m "Modified the index2.txt file"
                                                        =>git push origin main
